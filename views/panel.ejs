<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/panel.css">
    <title>Document</title>
</head>
<body>
    <div class="panel">
       <% for(let book of books){ %>
       <img src="<%=book.image_url%>" alt="book" class="panel-book"  data-id="<%=book.id %>">
      <% }%>

    </div>

    <script>
      document.body.addEventListener("click",(e)=>{
         if(e.target.classList.contains("panel-book")){
           window.location.href=`/book/${e.target.dataset.id}`;
         }
      })
    </script>
</body>
</html>